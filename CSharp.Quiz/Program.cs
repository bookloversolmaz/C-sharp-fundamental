namespace CSharp.Quiz;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("No!");
        Console.WriteLine("Compilation is when the code is translated all at once into machine code.");
        Console.WriteLine("Main tells program to execute everything within, without needing to call the method separately");
        Console.WriteLine("void, where my heart is, no it means that the program does not return anything to the os");
    }
}
